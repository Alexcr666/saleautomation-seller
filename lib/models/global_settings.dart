import 'cart_info.dart';
import 'seller_settings.dart';

class GlobalSettings {
  CartInfo cartInfo;
  SellerSettings sellerSettings;

  GlobalSettings({
    this.cartInfo,
    this.sellerSettings,
  });
}
